[project]
name = "rag-chat-and"
version = "0.1.0"
description = "PDF Upload and Chat with Unstructured Workflow"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.1.1",
    "requests>=2.32.5",
    "unstructured-client>=0.42.3",
    "streamlit>=1.28.0",
    "google-cloud-storage>=2.10.0",
]
